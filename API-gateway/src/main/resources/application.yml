spring:
    application:
     name:
      API-gateway

    cloud:
      gateway:
        routes:
          - id: api-1
            uri: lb//welcome-api
            predicates:
              - Path=/welcome
          - id: api-2
            uri: lb://greet-service
            predicates:
              - Path=/greet
server:
  port: 3333
